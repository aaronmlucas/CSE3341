procedure t5 is
	procedure A(x) is
		object y;
		y = new object(default, 1); 
		y[default] = 8; 
		out(x[default]); 
		x : y; 
	end
	integer m;
	integer n;
	object z;
begin
	m = 10;
	n = 5;
	z = new object(default, 1);
	z[default] = m+n;
	begin A(z);
	out(m);
end
