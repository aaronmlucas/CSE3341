procedure t4 is
	object a;
	object b;
	object c;
	procedure A(x) is
		b : x;
		c : x;
		out(x[default]);
	end
begin
	a = new object(default, 1);
	b = new object(default, 1);
	c = new object(default, 1);
	a[default] = 99;
	begin A(a);
end
