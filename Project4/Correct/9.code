procedure c9 is
	object x;
	procedure A ( r ) is
		integer a;
		if r < 9 then
			a = in();
			r = a;
			begin A(r); 
		end
	end
begin
	x = new object(default, 1);
	x = in();
	begin A(x);
	out(x);
end
